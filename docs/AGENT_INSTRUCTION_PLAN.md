# Инструкция для Агента: Создание плана (tasks/T-XXXX.md)

**Твоя роль:** Ты формируешь контракт на реализацию задачи до написания какого-либо кода.

## 🛑 Базовые правила (Plan Gate)
1. **План — это первый коммит.** Создавай файл `tasks/T-XXXX.md` (где XXXX — номер Issue) в новой ветке.
2. **Никакого кода до апрува.** Строго запрещено изменять файлы в `src/` и `tests/` до получения лейбла `plan:approved` на PR.
3. **Краткость.** План должен содержать 3–7 проверяемых шагов (чекбоксов). Не пиши куски кода в плане.

## 📝 Шаблон файла T-XXXX.md
Строго используй эту структуру:

```markdown
# T-XXXX: [Название задачи]

**Issue:** #[Номер]
**Spec:** [R-XXX | New requirement | n/a]

> **Когда использовать `n/a`?**
> Для задач, которые **не изменяют поведение системы** и не затрагивают `spec.md`.
> Это типично для `chore`/инфраструктуры/техдолга, рефакторинга, написания тестов,
> обновления CI, документации и т.п. В таких случаях в поле `Why` следует раскрыть
> мотивацию, поскольку единственный контракт — это сам PR body.

## Why
* [1-2 предложения: зачем это нужно бизнесу или системе?]

## Goal
* [Что конкретно изменится после выполнения?]

## Plan
* [ ] Шаг 1: [Например, добавить R-XXX в spec.md]
* [ ] Шаг 2: [Написать падающий тест для R-XXX]
* [ ] Шаг 3: [Реализовать логику в src/...]
* [ ] Шаг 4: [Проверить прохождение тестов]

## Acceptance
* [ ] [Критерий 1: проверяемый результат]
* [ ] [Критерий 2: успешный тест]
```

## 🔄 Сценарии работы
* **Старт задачи:** Создай ветку -> Напиши `T-XXXX.md` -> Открой PR -> Жди `plan:approved`.
* **Test-First:** Всегда включай шаг написания/обновления тестов *до* шага реализации логики.
* **Изменение требований в процессе (Re-plan):** Если во время написания кода (статус `in-progress`) ты понимаешь, что план не работает — **остановись**. Обнови `T-XXXX.md`, напиши комментарий в PR и жди повторного `plan:approved`.

## 🧹 Особые случаи: chore / техдолг / инфраструктура
Когда задача не меняет пользовательского поведения и не затрагивает `spec.md`, процесс остаётся прежним, но есть нюансы:

1. **Issue и заголовок.** Используй префикс `chore(area): …` (пример: `chore(db): migrate to connection pooling`).
2. **PR в две фазы.** Первый коммит — план-only. Создаёшь `tasks/T-XXXX.md`, описываешь саму работу и, важно, как будешь доказывать, что поведение **не изменилось** (обычно через прогон существующих тестов или добавление тестов-репортажей).
3. **Тело PR.** Авторы заполняют обязательные поля:
   * `Fixes: #123` (номер issue)
   * `Spec: n/a` (или `Internal change`)
   * `Plan: tasks/T-XXXX.md`
   * `Why:` 2–5 строк технической мотивации; для chore‑задач это ключевой текст, объясняющий причину изменения.
4. **FSM и Plan Gate.** Даже для технических работ проходишь через ту же цепочку:
   * Открываешь план-only PR (issue остаётся в `status:backlog`).
   * Ждёшь, пока команда поставит `plan:approved`.
   * Переводишь issue в `status:todo` или `status:in-progress` и пушишь последующие коммиты с реализацией.

Эта структура гарантирует, что техдолг документирован, планируется и проверяется так же серьёзно, как новые фичи.