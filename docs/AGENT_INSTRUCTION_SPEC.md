# Инструкция для Агента: Работа со спецификацией (spec.md)

**Твоя роль:** Ты отвечаешь за поддержание `spec.md` как единственного источника истины о поведении системы.

## 🛑 Базовые правила
1. **Никакого кода без спецификации.** Любое изменение поведения должно быть описано в `spec.md`.
2. **Декларативность.** Описывай *что* делает система, а не *как* она это делает.
3. **Тестируемость.** Каждое требование должно однозначно проверяться автотестом.

## 📝 Формат требований
Используй строгий формат RFC 2119 (`MUST`, `SHOULD`, `MAY`) и уникальные ID (`R-XXX`):

```markdown
* **R-XXX:** `[Component]` **MUST** `[Action/Behavior]` **WHEN** `[Condition]`.
```
*Пример:* `* **R-021:** API MUST return 404 WHEN run_id does not exist.`

## 🔄 Сценарии работы
* **Новая фича:** Найди последний номер `R-XXX`, добавь новое требование `R-XXX+1` в соответствующий раздел `spec.md`.
* **Баг без спецификации (неописанное поведение):** Сначала добавь новое требование `R-XXX` (узаконь ожидание), только потом планируй фикс.
* **Изменение логики:** Обнови существующее `R-XXX` или пометь его как `DEPRECATED` и создай новое.
* **Рефакторинг/Техдолг (chore):** Не трогай `spec.md`, если поведение системы не меняется.

## ⚠️ Ограничения
* Изменяй `spec.md` **только** в первом коммите PR (вместе с планом `tasks/T-XXXX.md`).
* Не приступай к реализации кода, пока изменения в `spec.md` не получат лейбл `plan:approved`.